<%= error_messages_for(@database) %>
<div id="mx-content">
  <div class="contextual">
    <%= link_to l(:button_back), project_mx_database_path(@project, @database),  class: 'icon icon-cancel' %>
  </div>
  <h2 class="mx-section-caption"><%= l('mx.title_databases_new') %></h2>
  <%= form_for(@database, url: project_mx_database_path(@project, @database), method: :put, html: { id: 'mx_database_form' }) do |f| %>
    <%= render partial: 'form', locals: { f: f, database: @database } %>
    <%= mx_submit l(:button_update) %>
    <%= link_to l(:button_cancel), project_mx_database_path(@project, @database) %>
  <% end %>
</div>

<% html_title(l('mx.title_databases_edit')) %>

<% content_for :header_tags do %>
  <%= stylesheet_link_tag 'redmine_mx', plugin: 'redmine_mx' %>
<% end %>

